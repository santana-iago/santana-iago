# Generated GitHub statistics

The `Generate profile summary cards` workflow replaces `github_dark/0-profile-details.svg` with the current profile statistics using the `github_dark` theme.

The dark summary card is intentionally used in both GitHub appearance modes, matching the visual structure of the reference profile.
